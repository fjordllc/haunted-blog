<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to "Haunted Blog", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li><%= link_to "My Blogs", my_blogs_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-link">
            <%= current_user.nickname %>
            <% if current_user.premium? %>
              (Premium)
            <% end %>
          </li>
          <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: 'nav-link btn btn-link' %></li>
        <% else %>
          <li><%= link_to "Sign In", new_user_session_path, class: 'nav-link' %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

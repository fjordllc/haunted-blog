<%= render @blog %>

<div>
  <%= link_to "Edit this blog", edit_blog_path(@blog) %> |
  <%= link_to "Back to blogs", blogs_path %>

  <div class="actions">
    <%= button_to "Destroy this blog", @blog, method: :delete, class: 'btn btn-danger', form: { data: { turbo_confirm: "Are you sure?" } } %>
  </div>
</div>
